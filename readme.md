**Graphical user interface for OpenAI's text to speech model. Built with PySimpleGUI.**

Install requirements:
```
python3.10 -m pip install -r requirements.txt
```

Run program:
```
python3.10 ./main.py
```
